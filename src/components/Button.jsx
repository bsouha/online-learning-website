import React from "react";

const Button = ({ styles }) => (
  <button type="button" className={`py-2 px-20 mt-1 mb-50 font-poppins font-medium text-[18px] text-Black bg-botton__style rounded-[10px] outline-none ${styles}`}>
Sign Up  </button>
);

export default Button;
